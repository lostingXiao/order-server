
// 获取店铺列表
const getUserInfoByIdService = ({offset, limit, keyword}) => {
  // return Group.findAndCountAll({
  //     order: [['id', 'desc']],
  //     offset,
  //     limit,
  //     where: {
  //         name: {
  //             [Op.like]: `%${keyword}%`
  //         }
  //     }
  // })
}

module.exports={
  getUserInfoByIdService
}